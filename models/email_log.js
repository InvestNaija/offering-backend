module.exports = (sequelize, Sequelize) => {
    const Email_Log = sequelize.define('email_log', {
        id: {
            allowNull: false,
            pri
        }
    })

    return Email_Log;
}